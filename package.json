{
  "name": "test",
  "version": "0.1.1",
  "scripts": {
    "release": "generate-release"
  },
  "config": {
    "generateRelease": {
      "files_to_version": ["README.md", "bower.json"]
    }
  },
  "devDependencies": {
    "generate-release": "^0.10.0"
  }
}
