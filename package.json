{
  "name": "test",
  "version": "0.2.3",
  "scripts": {
    "release": "generate-release"
  },
  "config": {
    "generateRelease": {
      "files_to_version": [
        "README.md",
        "bower.json"
      ],
      "release_message": "v{version}"
    }
  },
  "devDependencies": {
    "generate-release": "^0.10.0"
  }
}
