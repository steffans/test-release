{
  "name": "test",
  "version": "0.2.6",
  "scripts": {
    "release": "generate-release"
  },
  "config": {
    "generateRelease": {
      "release_message": "v{version}",
      "files_to_commit": [
        "./bower.json"
      ],
      "pre_commit_commands": [
        "node ./release.js"
      ]
    }
  },
  "devDependencies": {
    "generate-release": "^0.10.0",
    "replace-in-file": "^1.1.1"
  }
}
