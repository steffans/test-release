{
  "name": "test",
  "version": "0.3.0",
  "scripts": {
    "release": "generate-release"
  },
  "config": {
    "generateRelease": {
      "release_message": "v{version}",
      "pre_commit_commands": [
        "node ./release.js"
      ],
      "files_to_version": [
        "README.md",
        "bower.json"
      ]
    }
  },
  "devDependencies": {
    "generate-release": "^0.10.0",
    "replace-in-file": "^1.1.1"
  }
}
